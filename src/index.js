import React from 'react';
import ReactDOM from 'react-dom';
import JournaldApp from './JournaldApp';

ReactDOM.render(
  <React.StrictMode>
    <JournaldApp />
  </React.StrictMode>,
  document.getElementById('root')
);